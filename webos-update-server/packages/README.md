If you're feeling daring, rename `manifest.json` to `manifest-off.json` 
then `manifest-openssl.json` to `manifest.json` and run the server.

This would attempt to deploy an OpenSSL update to the TouchPad. I have no idea if it will work!
